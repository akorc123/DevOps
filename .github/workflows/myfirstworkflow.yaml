name: My cool Workflow

on:
  push:
  pull_request:
  workflow_dispatch:
 
jobs:
    myfirstjob:
              name: First job
              runs-on: ubuntu-latest
              steps:
              - name: Step One
                uses: actions/checkout@v3
              - name: Step Two
                run: |
                  pwd
                  ls
                  cd capstone_project/hello-world/
                  npm test
